Module card styled as a block in a chain; lifts and rings in the course accent on hover.

```jsx
<ModuleBlock chained index="MODULE 02" title="Hashing & Integrity"
  meta="SHA-256, collisions, and why you never store a raw password."
  hash="0x9f3a2b8e00a1…" />
```

Set `chained` on every block after the first so the notches read as one chain.
