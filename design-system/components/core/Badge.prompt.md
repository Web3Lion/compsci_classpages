Uppercase mono tag for difficulty, status and category; tinted fill with a matching border.

```jsx
<Badge tone="bad">Hard</Badge>
<Badge tone="amber" pill>ELECTIVE · SEMESTER</Badge>
```

Map difficulty consistently: Easy → `ok`, Medium → `warn`, Hard → `bad`.
