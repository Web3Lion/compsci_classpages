Text field with an uppercase mono label above it.

```jsx
<Input label="Class code" mono placeholder="CSP-P3-2K8R" />
<Input label="Handle" error="That name isn't allowed." />
```

Use `mono` for anything the student copies or types verbatim (codes, flags, PINs).
