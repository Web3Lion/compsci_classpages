Bordered surface at one of three nesting levels; `interactive` adds the signature 3px lift and accent ring.

```jsx
<Card level="standard">
  <Card level="inner">Nested content steps the surface down</Card>
</Card>
```

Never nest a standard card inside a standard card — use `inner`.
